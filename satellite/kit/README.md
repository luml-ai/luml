# LUML Satellite Kit

`luml-satellite` provides the shared wire contract and runtime building blocks for LUML
satellites. Install the `serving` or `monitoring` extra only when that functionality is
needed.

