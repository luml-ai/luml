# LUML Deploy Satellite

Model deployment worker agent for LUML platform.

## Installation

```bash
uv venv && source .venv/bin/activate && uv sync
```

## Usage

```bash
python -m deploy_satellite
```
