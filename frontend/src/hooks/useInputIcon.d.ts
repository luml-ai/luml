import { type Ref } from 'vue';
export declare const useInputIcon: (inputs: Ref[], formRef: any, values: Ref, isShowEditIcon?: boolean) => {
    getCurrentInputIcon: any;
    onIconClick: (inputName: string) => void;
};
