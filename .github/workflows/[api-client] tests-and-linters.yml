name: Api Client Tests and Linters

on:
  push:
    branches: [ main ]
    paths:
      - 'sdk/python/api/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'sdk/python/api/**'
  workflow_dispatch:

jobs:
  checks:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sdk/python/api
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies with uv
        run: |
          python -m pip install --upgrade uv
          uv sync --group dev

      - name: Check formatting
        run: uv run ruff format --check luml_api tests examples

      - name: Linting
        run: uv run ruff check luml_api tests

      - name: Type checking
        run: uv run mypy luml_api

      - name: Running tests
        run: uv run pytest